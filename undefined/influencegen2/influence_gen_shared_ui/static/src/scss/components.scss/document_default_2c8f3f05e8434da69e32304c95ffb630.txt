//------------------------------------------------------------------------------
// Main SCSS file for importing all shared component styles.
// This file is included in Odoo's asset bundles (web.assets_common)
// to make all shared and component-specific styles available.
//------------------------------------------------------------------------------

// 1. Import Shared SCSS Foundations
// These files provide the base variables, mixins, and global utilities
// that individual component SCSS files will rely on.
// The order matters: variables first, then mixins, then global styles.
// Note: Odoo's asset bundler should make these available.
// Explicit imports here ensure that if this file were processed by a standalone
// SCSS compiler, or if the bundler's dependency resolution is strict,
// the dependencies are clear.

@import 'shared_variables';
@import 'shared_mixins';
@import 'shared_global';

// 2. Import Individual Component Styles
// Each shared UI component should have its own SCSS file, which is imported here.
// This keeps component-specific styles modular and organized.
// The paths are relative to this `components.scss` file.

@import '../components/campaign_card/campaign_card.scss';
@import '../components/metric_tile/metric_tile.scss';
@import '../components/data_chart_wrapper/data_chart_wrapper.scss';
@import '../components/loading_indicator/loading_indicator.scss';
@import '../components/status_badge/status_badge.scss';
@import '../components/profile_snippet/profile_snippet.scss';

// Add @import statements for any new shared UI components below.
// For example:
// @import '../components/new_component/new_component.scss';

// Note on Odoo's SCSS compilation:
// Odoo's asset management system will compile these SCSS files into CSS.
// Ensure that all imported files exist and paths are correct.
// Using `web.assets_common` for these styles ensures they are available
// in both the Odoo backend and frontend (portal).